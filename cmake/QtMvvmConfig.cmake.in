@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

find_dependency(Qt${QT_VERSION_MAJOR} COMPONENTS Core Gui Widgets)

if(NOT TARGET QtMvvm::Core)
    include("${CMAKE_CURRENT_LIST_DIR}/QtMvvmTargets.cmake")
endif()

check_required_components(QtMvvm)
